{% extends 'base.html.twig' %}

{% block title %}Licencie{% endblock %}

{% block body %}
    <h1>Licencie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ licencie.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ licencie.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ licencie.lastName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ licencie.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_licencie_index') }}">back to list</a>

    <a href="{{ path('app_licencie_edit', {'id': licencie.id}) }}">edit</a>

    {{ include('licencie/_delete_form.html.twig') }}
{% endblock %}
